{
  "name": "chatgpt-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp app.html README.md CHANGELOG.md QA-rapport.md dist/ && cd dist && zip -q app.zip app.html README.md CHANGELOG.md QA-rapport.md && cd ..",
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}

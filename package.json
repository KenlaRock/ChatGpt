{
  "name": "chatgpt-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp app.html app.js app.css README.md CHANGELOG.md QA-rapport.md dist/",
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}

# AI Gate live scope test

This harmless document is the only ordinary task path declared in the session proof. The same branch deliberately adds an undeclared root-level `dummy-file.md` so the active AI Gate must reject the pull request.

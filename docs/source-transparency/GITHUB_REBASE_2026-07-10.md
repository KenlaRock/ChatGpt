# GitHub Rebase Record — 2026-07-10

**Repository:** `KenlaRock/ChatGpt`  
**New canonical purpose:** NullForge Simulation Lab  
**Authorized by:** Ken Kängström  
**Operation:** full main-tree replacement

## Preservation

The previous repository head was preserved before replacement as:

`archive/pre-nullforge-rebase-2026-07-10`

The old repository content was explicitly declared non-canonical and without remaining project value. It was not merged into the new tree because doing so would contaminate the research repository with unrelated storyboard and deploy history.

## Binary artifact policy

Canonical WAV files are generated by the reference runner and CI, not committed to Git. The Drive foundation archive preserves the initial rendered artifacts. Metrics, snapshots, experiment recipes and hashes remain version controlled.

## Provenance chain

```text
Chat design source
→ foundation documents and schemas
→ deterministic reference experiment
→ Git main-tree rebase
→ CI-regenerated evidence
```

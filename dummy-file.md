# Deliberate out-of-scope dummy

This inert Markdown file is intentionally placed at the repository root and omitted from the session proof's approved scope. A functioning AI Gate must reject the pull request before tests run.
